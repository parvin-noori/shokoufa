export interface PostType {
  id?: string;
  image_url: string|null;
  slug:string|null;
  title: string;
  // minRead: number;
  // likesCount: number;
  // createdAt?: Date;
  // updatedAt?: Date;
}